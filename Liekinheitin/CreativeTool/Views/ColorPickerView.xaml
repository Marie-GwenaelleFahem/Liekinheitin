<UserControl x:Class="Liekinheitin.CreativeTool.Views.ColorPickerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource PanelBackground}" BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1">
        <StackPanel>
            <Border Background="{StaticResource PanelTitleBackground}" Padding="4">
                <TextBlock Text="Couleur" Foreground="White" FontWeight="Bold" />
            </Border>

            <StackPanel Margin="8">
                <Rectangle Height="40" Fill="{Binding CurrentColor, Converter={StaticResource ColorToBrush}}" Margin="0,0,0,8" />

                <TextBlock Text="R" Foreground="White" />
                <Slider Minimum="0" Maximum="255" Value="{Binding R}" Margin="0,0,0,4" />
                <TextBlock Text="G" Foreground="White" />
                <Slider Minimum="0" Maximum="255" Value="{Binding G}" Margin="0,0,0,4" />
                <TextBlock Text="B" Foreground="White" />
                <Slider Minimum="0" Maximum="255" Value="{Binding B}" Margin="0,0,0,8" />

                <ItemsControl ItemsSource="{Binding Palette}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="24" Height="24" Margin="2"
                                    Background="{Binding Converter={StaticResource ColorToBrush}}"
                                    MouseLeftButtonDown="OnPaletteClick" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>