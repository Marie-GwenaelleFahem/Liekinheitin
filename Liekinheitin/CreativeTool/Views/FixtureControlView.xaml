<UserControl x:Class="Liekinheitin.CreativeTool.Views.FixtureControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource PanelBackground}" BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1">
        <StackPanel>
            <Border Background="{StaticResource PanelTitleBackground}" Padding="4">
                <TextBlock Text="Projecteur et lyres" Foreground="White" FontWeight="Bold" />
            </Border>

            <StackPanel Orientation="Horizontal" Margin="8">
                <StackPanel Width="60" Margin="0,0,8,0" HorizontalAlignment="Center"
                            MouseLeftButtonDown="OnProjectorClick" Cursor="Hand">
                    <Border Width="32" Height="32" CornerRadius="16" Background="#FF888888" HorizontalAlignment="Center" />
                    <TextBlock Text="Projecteur" Foreground="White" FontSize="10" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Margin="0,2,0,0" />
                </StackPanel>

                <ItemsControl ItemsSource="{Binding MovingHeads}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Width="60" Margin="0,0,8,0" HorizontalAlignment="Center"
                                        MouseLeftButtonDown="OnHeadClick" Cursor="Hand">
                                <Polygon Points="16,0 32,16 16,32 0,16" Fill="#FF888888" Width="32" Height="32" HorizontalAlignment="Center" />
                                <TextBlock Text="{Binding Index, StringFormat='Lyre {0}'}" Foreground="White" FontSize="10" HorizontalAlignment="Center" Margin="0,2,0,0" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>